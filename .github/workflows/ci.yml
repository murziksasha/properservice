name: CI

on:
  push:
    branches: [main, master, groKmigrateNext]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test
        env:
          SESSION_SECRET: ci-test-session-secret
          ADMIN_PASSWORD: ci-test-password

      - name: Build
        run: npm run build
        env:
          SESSION_SECRET: ci-test-session-secret
          ADMIN_PASSWORD: ci-test-password
