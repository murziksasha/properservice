# Посібник адміністратора

## Вхід

1. Відкрийте `/admin/login`
2. Введіть пароль з `ADMIN_PASSWORD`
3. Якщо увімкнено 2FA — введіть 6-значний код з Authenticator
4. Після входу — Dashboard зі статистикою, **Live health** і швидкими діями

## Двофакторна автентифікація (2FA / TOTP)

Опційний другий фактор для входу в адмінку. Код генерує застосунок (Google Authenticator, Aegis, 1Password тощо).

### Увімкнення з адмінки (рекомендовано)

1. Увійдіть у адмінку (поки без 2FA)
2. **Налаштування → Безпека · 2FA**
3. Введіть пароль адмінки → **Увімкнути 2FA**
4. Відскануйте QR (або скопіюйте secret / otpauth URI) у Authenticator
5. Введіть 6-значний код → **Підтвердити і увімкнути**
6. Secret зберігається в `data/admin-totp.json` (не в `site.json`, не в git)
7. На **Огляд** у Live health з’явиться «2FA TOTP увімкнено»
8. Наступний логін — пароль + код з застосунку

### Вимкнення з адмінки

**Налаштування → Безпека · 2FA → Вимкнути 2FA** — пароль + поточний код 2FA. Видаляє `data/admin-totp.json`.

### Увімкнення через `.env` (ops / Docker)

Якщо задано `ADMIN_TOTP_SECRET`, він **має пріоритет** над файлом; UI setup/вимкнення недоступні.

1. Згенеруйте **base32**-secret (або візьміть з UI / `.env.example`: `JBSWY3DPEHPK3PXP`)
2. Додайте в Authenticator (TOTP, 30 с, 6 цифр) або:
   `otpauth://totp/ProperService:admin?secret=ВАШ_SECRET&issuer=ProperService&algorithm=SHA1&digits=6&period=30`
3. У `.env`:
   ```env
   ADMIN_TOTP_SECRET=ВАШ_SECRET
   ```
4. Перезапустіть Node / PM2 / Docker

### Вимкнення через `.env`

Приберіть або закоментуйте `ADMIN_TOTP_SECRET` і перезапустіть. Якщо лишився `data/admin-totp.json` — 2FA знову візьметься з файлу (вимкніть у UI або видаліть файл).

### Безпека

- Secret показується **один раз** під час setup (QR + копіювання)
- Confirm/disable вимагають пароль адмінки; disable — ще й валідний TOTP
- Off-site backup: якщо копіюєте весь `data/`, туди потрапить і `admin-totp.json` — тримайте backup захищеним
- Поки 2FA вимкнено, поле «Код 2FA» на логіні можна лишити порожнім

## Заявки

**Адмінка → Заявки** (Огляд у меню)

- Усі заявки з форми зворотного дзвінка (`data/leads.json`)
- Працює **навіть без SMTP** — телефон зберігається локально
- У записі: час, телефон, `pagePath` (звідки відправлено форму), чи пішов email, **нотатка** оператора
- Фільтр: Нові / Опрацьовані / Усі
- **Оброблено** / **Видалити** / click-to-call / нотатка (blur → save)
- Фільтри: статус, **сьогодні / 7 днів**, пошук по телефону, **CSV**
- UTM (якщо було в URL), історія дій (created / handled / note)

### Лист на пошту магазину (SMTP)

Після ліда (якщо налаштовано `SMTP_*` / `MAIL_TO`) приходить лист з службовою інформацією:

- телефон (і `tel:`-посилання), час, **ID заявки** (той самий, що в журналі)
- сторінка сайту та заголовок вкладки, Referer
- IP, User-Agent, мова браузера
- посилання на `/admin/leads` (якщо задано `SITE_URL`)

Без SMTP заявка все одно з’являється в журналі з міткою «без email».

## Замовлення

**Адмінка → Замовлення**

- Замовлення з **сторінки товару** (`/shop/[id]` → форма «Замовити»)
- Файл: `data/orders.json` (окремо від заявок)
- У записі: час, телефон, коментар, снапшот товару (назва, ціна, код), чи пішов email, **нотатка**
- Працює **навіть без SMTP** — замовлення лишається в журналі (`email ✓` / `без email`)
- Фільтр: Нові / Опрацьовані / Усі
- **Оброблено** / **Видалити** / click-to-call / нотатка / посилання на сторінку товару
- Фільтри: статус, період, телефон, **CSV**
- Live health на **Огляд** (потрібна сесія): SMTP, Telegram, 2FA, SITE_URL, кнопка **Тест SMTP**

### Як клієнт замовляє

1. Відкриває опублікований товар на сайті
2. Вводить телефон (+ опційний коментар)
3. Натискає **Замовити**
4. Менеджер бачить запис у **Замовлення** і (якщо SMTP) лист на `MAIL_TO`

Кількість завжди 1 — деталі узгоджуються по телефону. Кошика немає.

## Навігація адмінки

- Бічна панель **завжди видима** під час прокрутки контенту (скролиться лише основна колонка).
- На великих екранах кнопка **згорнути** (іконки ←→ біля логотипу) ховає підписи пунктів — лишаються **піктограми**; стан зберігається в браузері.
- На вузьких екранах — кнопка-меню (гамбургер) і виїжджаюча панель.
- **Inbox** (`/admin/inbox`) — єдина черга заявок + замовлень зі статусами workflow, SLA, шаблонами відповідей, історією по телефону. Клавіші: `j`/`k`, `c` дзвінок, `d` готово, `/` пошук.
- Бейджі лічильників у меню (polling ~20 с); опційно browser notifications.
- **Ctrl+K** — command palette: навігація, нещодавні, ★ обране, сторінки/товари/медіа, телефон.
- Breadcrumbs під шапкою контенту; нещодавні сторінки пишуться в localStorage.
- **?** — довідка гарячих клавіш; у меню «Клавіші» / «Компакт: ON|OFF» (щільність UI).
- Dashboard: черга дня, графік 30 днів, UTM/топ сторінок/товарів, activity feed, онбординг-чеклист.
- Inbox: multi-select bulk, CSV, **SSE live**, фільтри **Передзвінки** / **Дублікати** (повторні номери), бейдж «дубль».
- Dashboard: блок **Передзвінки** (заплановані callbackAt).
- Health: **Копіювати health** / **Export health** JSON.
- Сторінки: пошук, фільтр видимості, бейджі чернетки / SEO / HTML.
- Конструктор: **Зберегти чернетку** (live не чіпає) / **Опублікувати live** / відкрити / відхилити draft.
- Конструктор outline: **Diff vs live** (назва, meta, секції added/removed/changed, порядок).
- Inbox: **Telegram ↗** (одиничний + нотатка) і **Telegram bulk** для вибраних (до 25; `TELEGRAM_*` env).
- Конструктор Preview: кнопка **Live|Draft** — два iframe (публічний live + ephemeral draft).

## Медіатека

**Адмінка → Медіатека**

- Сітка файлів `public/uploads/` (метадані — `data/media-index.json`)
- Upload з автоматичною оптимізацією (JPEG → WebP; PNG зберігається; GIF без змін); **відео** MP4/WebM до 80 МБ (без resize)
- Фільтр **типу**: Усі / Фото / Відео
- **Тематичні папки** (віртуальні): створити / перейменувати / видалити зліва; файли лишаються `/uploads/імʼя` (URL не ламається). Видалення папки переносить файли в «Без папки».
- **Сортування**: новіші / за назвою / **вручну (DnD)** — ручний порядок у межах обраної папки (не в «Усі файли»).
- **Розмір** (пресет resize): Загальне, Товар, Логотип, Hero, OG (лише для фото)
- **Роль** (призначення): Товар, Hero, Блок сторінки, Лого, Відгуки, OG, Інше + **теги**
- У полях зображень / товару — **«З бібліотеки»** (фільтр папка + роль + тип)
- Copy URL / мета (роль, папка, теги) / видалення
- **Захист видалення:** якщо файл ще використовується в товарі, секції чи налаштуваннях — кнопка **Видалити** неактивна, tooltip пояснює де саме (🔗 N посилань)
- Нові файли доступні **одразу** (без `pm2 restart`)

## Меню

**Адмінка → Меню**

- **Шапка сайту** — пункти header (додати / видалити / порядок)
- **Навігація послуг** — вкладки послуг (`slug` → URL `/slug`)
- Перетягування за **⠿**, стрілки на клавіатурі (↑↓ на ⠿)
- **Ctrl+S** — зберегти

## Сторінки

**Адмінка → Сторінки**

- Додайте сторінку (назва + опційний slug)
- **видима** — публікація на сайті
- **Конструктор** — редагування секцій
- **↗** — відкрити публічну сторінку
- **⧉** — дублювати сторінку (копія прихована)
- Головну (`slug` порожній) **не можна** видалити

### Конструктор

- Додавання / приховування / **DnD** (⠿) / ↑↓ / **⧉** дубль секції
- **contentHtml**: якщо заповнено — на сайті лише HTML (секції ігноруються); порожньо → секції (політика тощо)
- **Preview** — iframe зі збереженою версією (оновлюється після Save)
- HTML-заголовки, зображення (URL + upload + preview)
- Розмір заголовків і масштаб тексту
- **Hero**: рядки «про сервіс», форма callback, active slug, зображення
- **Contacts**: телефони/соцмережі секції, кнопка **↻ З налаштувань**; порожні списки → fallback з Налаштувань на сайті
- **Callback / shop-grid**: заголовок, кнопка, placeholder / subtitle
- **Ctrl+S** + попередження при закритті вкладки **і** переходах по адмін-меню
- Збереження з **updatedAt**: конфлікт іншого сеансу → повідомлення оновити сторінку

## Товари

**Адмінка → Товари**

- Додайте товар: назва, ціна, **код** (опційно, мін. 2 символи), **категорія / група** (опційно; **порожньо = «Інше»**), опис
- **Фото товару** (єдина сітка):
  - **Завантажити** (кілька одразу) або **З бібліотеки**
  - **Перше фото = головне** (бейдж «Головне»); ★ на інших — зробити головним
  - **Перетягування** плиток (або ← →) задає порядок на сайті; макс. 12 фото
  - × **лише відв’язує** фото від картки (файл у медіатеці лишається)
- **Відео-огляд** (опційно): upload MP4/WebM (до 80 МБ) або вибір з бібліотеки; на сторінці товару — плеєр
- **Пошук** по назві, опису, категорії (включно з «Інше») й коду
- **Фільтри:** видимість (Усі / Опубліковані / Приховані), **категорія** (select + chips; серед них завжди **«Інше»**, якщо є товари без конкретної категорії)
- Кнопка **«Скинути фільтри»** — повертає пошук, видимість, категорію й сортування до значень за замовчуванням (завжди видима, неактивна коли вже «чисті» фільтри)
- **Режими списку:** «Групи» (за категоріями, як на сайті) або «Плоский»
- У рядку: прев’ю, ціна, код, pill категорії (для без категорії — **«Інше»**), бейдж **Опубліковано / Приховано**, швидкий тогл 👁, редагування, дублікат, видалення
- Приховані рядки приглушені (dashed border) — статус видно без відкриття форми
- **Перейменувати** групу в шапці секції — bulk-оновлює `category` у всіх товарах групи (групу «Інше» перейменувати не можна — це дефолтний кошик)
- **Сортування списку** (ціна, назва) — лише для перегляду в адмінці, **не** змінює порядок на сайті
- **DnD ⠿** / ↑↓ для порядку в каталозі — коли пошук порожній, видимість «Усі», без фільтра категорії і сортування «За порядком каталогу». Інакше handle сірий і є підказка чому
- **«Опубліковано» / тогл** — лише видимість на `/shop`; **фото/відео не видаляє**
- **Видалення товару (×)** — після підтвердження:
  1. картка зникає з каталогу;
  2. файли `/uploads`, які були **лише** в цій картці, видаляються з бібліотеки;
  3. якщо фото/відео ще в іншому товарі (або секції/налаштуваннях) — **не видаляється** (у confirm і toast видно чому)
- «Зберегти всі» / Ctrl+S записує список (порядок, тогли); видалення товару зберігає одразу
- На публічній сторінці товару — галерея (клік по мініатюрах), відео-огляд (якщо є), форма **Замовити**

### Публічний магазин `/shop`

- Пошук товару, сортування (порядок каталогу / ціна ↑↓ / назва), фільтр за категорією (включно з **«Інше»**)
- Параметри в URL: `?q=…&sort=price-asc&category=…` (можна ділитися посиланням; `category=Інше` — товари без конкретної категорії)
- Кнопка **Скинути** очищає фільтри
- При **порядку каталогу** без пошуку/категорії й ≥2 категоріях — товари показуються **секціями-групами** (як в адмінці; «Інше» — остання секція)

## Секція «Відгуки» (feedback)

Секція на сторінці (конструктор сторінок) + посилання в **Налаштування → reviewsUrl**.

### Слайдер — фіксований розмір

- На публічному сайті карусель має **фіксовану** ширину й висоту за **найбільшим** скріном у наборі (висота + ширина контенту).
- Усі слайди лежать в одному боксі: коротші/вужчі **центруються** (`object-fit: contain`), layout **не стрибає** при перемиканні.
- Орієнтир за замовчуванням — найвищий скрін у наборі (наприклад `feed-3.jpg` з відповіддю власника). Нові зображення краще готувати **в одному кадрі** (або не більшими за еталон), щоб не роздувати бокс.
- Media purpose у бібліотеці: **`feedback`** («Скріни / фото відгуків»).

### Контент у адмінці

- **Сторінки → секція «Відгуки»**: список зображень, текст кнопки («Більше відгуків» тощо).
- **Налаштування → Посилання на відгуки**: URL Google / зовнішніх відгуків — без нього кнопка на сайті не показується.

## Налаштування

**Адмінка → Налаштування**

- SEO title / description, логотип, favicon
- Години, адреса, email, copyright, map, **посилання на відгуки** (`reviewsUrl`)
- **Телефон шапки** + **додаткові телефони** (список)
- **Соцмережі** (Viber / Telegram / Instagram / YouTube + іконка)
- Політика конфіденційності
- **Backup**: експорт / імпорт / server snapshots

## План поліпшень

Покроковий roadmap: [admin-improvement-plan.md](./admin-improvement-plan.md).

### Нові розділи

- **Клієнти** `/admin/clients?phone=` — картка за номером
- **Активність** `/admin/activity` — audit log
- **Ops** `/admin/ops` — аварійний runbook + SOP лідів
- Inbox: **Взяв у роботу**, фільтри **Мої** / **Без assignee**, outcome + нотатка, snooze, шаблони
- Публічні форми: телефон у `+380…`; повторна заявка з тим самим open-номером → note в існуючий lead (dedup)
- Конструктор: **live publish лише owner**; editor — чернетка / «На ревʼю»; фільтр сторінок «На ревʼю»
- Dashboard: ранковий/вечірній digest → TG, метрики first-touch, каталог-проблеми, publishAt-календар
- Конструктор: publish gate (SEO/diff confirm), «На ревʼю», scheduled publish, hide 📱/🖥
- Товари: confirm зміни ціни ≥20%, checklist перед visible, related picker, історія цін
- Налаштування: notify prefs; ops alerts + SLA (cron `/api/ops-alerts`, digest `/api/digest`)

## Користувачі / ролі (опційно)

**Налаштування → Користувачі та ролі**

- За замовчуванням лишається один вхід через `ADMIN_PASSWORD` (legacy owner).
- Можна створити multi-user у `data/admins.json`: ролі `operator` (inbox), `editor` (контент), `owner`.
- На логіні поле «Логін» — опційне; step-up паролем власника для створення/видалення користувачів і revoke sessions.
- Список сесій + «Вийти скрізь».
- **operator** бачить лише Огляд / Inbox / Заявки / Замовлення; API контенту/медіа повертає 403.
- **editor** — контент + ops, без multi-user і restore backup.
- Idle-попередження після ~6 год без активності.

## Товари (merchant)

- Bulk multi-select: опублікувати / приховати / категорія / % ціни / видалити
- CSV import/export (колонки: id, code, title, price, category, visible, inStock, badge, promoText…)
- Поля: **inStock**, **badge** (hit/sale/new), **promoText**, **sortPin** (закріпити зверху каталогу)
- На публічному `/shop` — бейджі, «немає в наявності», промо-рядок

## Конструктор / preview

- **Live draft** — preview незбереженої сторінки (`/admin/preview/{token}`) без публікації
- Outline секцій, SEO hints, шаблони блоків, ревізії, rich text для hero
- Ctrl+Z undo · conflict force overwrite

## Медіатека (додатково)

- Orphans filter + bulk purge, Alt з назви, replace-in-place (↻)
- Focus X/Y % (object-position) у метаданих файлу

## Backup (laptop / Keen DNS)

1. Кожне **Зберегти** створює auto-snapshot у `data/backups/` (оберіть `BACKUP_KEEP`)
2. **Налаштування → ⬇ Експорт JSON** — файл на ПК
3. **Snapshot** — ручна копія на сервері
4. **Restore** — відновлення з snapshot (спочатку pre-restore копія поточного стану)
5. **×** — видалити snapshot; **Імпорт** — з файлу (Zod)
6. CLI: `npm run backup` / HTTP cron — див. `docs/deploy.md`

Також копіюйте `data/site.json`, `data/leads.json`, `data/orders.json`, `data/backups/`, `public/uploads` **на інший диск** (off-site) — див. `docs/deploy.md`.

Для sitemap/OG/JSON-LD у production задайте `SITE_URL` (повний URL сайту).

## Поради

- Після збереження Preview оновлюється сам; публічну вкладку — Ctrl+F5
- Зображення ≤ 5 MB: JPEG, PNG, WebP, GIF; на upload JPEG → WebP, розмір за пресетом поля (товар / лого / hero / …)
- Не вставляйте `<script>` у HTML — санітизуються при показі
- Для LAN: `COOKIE_SECURE=false`; опційно `ADMIN_IP_ALLOWLIST`
- На мобільному сайті — sticky «Зателефонувати»
- Публічний сайт показує cookie-банер знизу (стиль як у решти chrome). Відповідь (**Прийняти** / **Лише необхідні**) зберігається на цьому пристрої (`localStorage` + cookie `ps-cookie-consent`) і не синхронізується між телефонами/ПК. Повторно відкрити банер — посилання «Налаштування cookie» у футері. Текст політики — з **Налаштування** (`privacyPolicyUrl` / `privacyPolicyText`). Віджети Gincore підключаються лише після «Прийняти».
